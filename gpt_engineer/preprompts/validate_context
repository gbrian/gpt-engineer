RETURN A VALUE FROM 0 TO 1 ABOUT HOW RELEVANT IS THE CONTEXT FOR THE TASK.
0 MEANS TOTALLY IRRELEVANT.
RETURN ONLY A VALUE FROM 0 TO 1 ANYTHING ELSE WILL MAKE SYSTEM FAIL.

TASK:
{prompt}

CONTEXT:
{context}
