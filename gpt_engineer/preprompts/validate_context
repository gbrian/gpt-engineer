# INSTRUCTIONS
ANALIZE HOW USEFULL IS THE CONTEXT BELOW RELATED WITH THE TASK. 

TASK:
"{{ prompt }}"


CONTEXT:
{{ context }}


RETURN A JSON OBJECT WITH THE SCORE AND TAGS LIKE THIS:
```json
{ 
  "score": 0.3,
  "tags": "code, html, style, ..." 
}
```