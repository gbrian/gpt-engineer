# INSTRUCTIONS
ANALIZE HOW USEFULL IS THE CONTEXT BELOW RELATED WITH THE TASK. 
RETURN A JSON OBJECT WITH THE SCORE LIKE:
```json
{ "score": 0.3 }
```

# TASK IS:
{{ prompt }}


# CONTEXT IS:
{{ context }}
